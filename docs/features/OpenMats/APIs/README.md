# Open Mat API PRDs

Open Mat API requirements should live in this folder.

Current Open Mat create, edit, and delete behavior is documented through page and product PRDs because the implementation uses admin server actions rather than dedicated API route PRDs.
